appId: com.echoflow.app
productName: EchoFlow
directories:
  output: release
  buildResources: build
files:
  - dist/**/*
  - electron/**/*
  - package.json
mac:
  category: public.app-category.productivity
  target:
    - dmg
    - zip
  # icon: build/icon.icns  # Uncomment when build/icon.icns exists
dmg:
  title: EchoFlow
  # icon: build/icon.icns  # Uncomment when build/icon.icns exists
